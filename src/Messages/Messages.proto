syntax = "proto3";

enum MessageType {
    // reserve from 0-9 for operation messages

    // from 10+ for room messages
    JOIN_ROOM = 10;
    LEAVE_ROOM = 11;
}

message Client_JoinRoon {
    string roomName = 1;
    string roomID = 2;
}

message Client_LeaveRoom {
    string roomID = 1;
}